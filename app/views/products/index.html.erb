<h1>Products</h1>
<% @products.each do |p| %>
  <h2><%= p.name %></h2>
  <p><%= p.description %></p>
  <%= link_to "View Product", product_path(p) %></br>
  <%= link_to 'Delete', p, method: :delete, data: { confirm: 'Are you sure?' } %></br>
<% end %>

<%= link_to "New Product", new_product_path %>